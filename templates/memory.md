# {{HOST}} — hunt log
created: {{DATE}}

> Append-only. Each session adds a `## <iso-date>` block.
> Format inside a block: short bullets, what tried + what observed.
> Do not paste full responses — store evidence in finding files.

